version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./customs_app.db
    volumes:
      - ./customs_app.db:/app/customs_app.db
      - ./data:/app/data
    restart: unless-stopped
